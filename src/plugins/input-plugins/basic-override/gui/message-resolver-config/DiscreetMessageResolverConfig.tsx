import { DiscreetMessageResolverDTO } from '../../message-resolver/discreet-message-resolver';

type PropTypes = {
  resolver: DiscreetMessageResolverDTO;
};

export default function DiscreetMessageResolverConfig(props: PropTypes) {
  const { resolver } = props;
  return <></>;
}

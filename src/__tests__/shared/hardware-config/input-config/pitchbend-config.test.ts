describe('fromDriver', () => {
  test('sets values correctly', () => {});
});

describe('toJSON', () => {
  test('de/serializes correctly', () => {});
});

describe('id', () => {
  test('returns correct id', () => {});
});
